import type { CommunityEvent } from '../types';

/**
 * No events are fabricated. Real events will be added here (or migrated to
 * Supabase `events` table) by administrators once confirmed by the
 * association.
 */
export const events: CommunityEvent[] = [];
