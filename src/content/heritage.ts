import type { HeritageItem } from '../types';

/**
 * The digital archive starts empty. Items will be added by administrators
 * once verified content, metadata, and media are supplied by the
 * association. See src/pages/admin for the management interface.
 */
export const heritageItems: HeritageItem[] = [];

export const heritageIntro = "Documentation culturelle vérifiée à venir, fournie par l'association.";
