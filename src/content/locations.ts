import type { CulturalLocation } from '../types';

/**
 * Placeholder location data structure. Culturally sensitive location
 * information (villages, chiefdoms) will be added only once verified by
 * the association.
 */
export const locations: CulturalLocation[] = [];
